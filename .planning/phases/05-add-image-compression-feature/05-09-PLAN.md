---
phase: 05-add-image-compression-feature
plan: 09
type: execute
wave: 4
depends_on: ["05-06", "05-07", "05-08"]
gap_closure: true
files_modified: []
autonomous: false
requirements: [D-02, D-04, D-05, D-06, D-07, D-08, INFRA-17, INFRA-18]
must_haves:
  truths:
    - "UAT Test 5 re-passes: an already-optimized PNG never grows after compression"
    - "UAT Test 9 re-passes: Cancel stops the in-flight row spinning"
    - "UAT Test 10 re-passes: re-dropping the same image writes -compressed-1"
    - "UAT Test 6 re-passes: the 'Re-compress at {n}%' button gives the quality slider a clear effect"
  artifacts: []
  key_links: []
---

<objective>
Re-verify the four UAT gaps (Tests 5, 6, 9, 10) against a freshly-built binary now that 05-06,
05-07, and 05-08 have landed. The original failures were found by human UAT; closure must be
confirmed by human UAT on a clean build (MEMORY 'Stale DerivedData builds': "tool missing" /
stale-binary artifacts are common — build clean and launch by full path from BUILD_DIR).

Purpose: Confirm the gap closure actually fixed the user-visible behavior, not just the tests.
Output: Updated 05-UAT.md results for Tests 5, 6, 9, 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-add-image-compression-feature/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean build + full test gate</name>
  <files>none — build/test gate only</files>
  <action>
    Run a clean build and the full test suite as the automated pre-checkpoint gate. This is the
    authoritative correctness gate before human re-test (single-test runs can false-GREEN via stale
    incremental compilation — MEMORY 'Stale DerivedData builds'). Capture the canonical BUILD_DIR so the
    human launches the freshly-built app (not a stale DerivedData copy).
  </action>
  <verify>
    <automated>xcodebuild clean build test -scheme Flint -destination 'platform=macOS' 2>&1 | tail -25</automated>
  </verify>
  <done>Clean build succeeds and the full FlintTests suite is green, including 05-06 never-larger tests, 05-07 cancellation test, and 05-08 recompress tests. BUILD_DIR path recorded for the human-verify step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human re-test of UAT Tests 5/6/9/10</name>
  <what-built>
    Gap closure for the four Phase-05 UAT failures:
    - Test 5 (GAP 1): never-larger-than-original guard on both PNG and non-PNG paths.
    - Test 9 (GAP 3): Cancel now stops in-flight work and resolves the spinning row.
    - Test 10 (GAP 4): clean re-drop disambiguates and writes -compressed-1 (was masked by the cancel bug).
    - Test 6 (GAP 2): explicit "Re-compress at {n}%" button applies a changed quality to the existing batch.
  </what-built>
  <how-to-verify>
    Quit any running Flint, then launch the FRESHLY-built app from the BUILD_DIR recorded in Task 1
    (open the .app by full path — do NOT launch a stale copy). Open the Image Compressor tool, then:

    1. TEST 5 — Drop an already-optimized PNG (e.g. the Google 2015 logo, ~30 KB, low color count,
       transparency). Expected: the compressed file is NOT larger than the original (saved % is 0% or
       positive, never "+NN%"); transparency is preserved; output is a valid same-size PNG.
    2. TEST 9 — Drop one LARGE photographic image and press Cancel while it is compressing. Expected:
       the spinner STOPS (row leaves the spinning state); the Cancel button stays visible until the row
       resolves, then hides. No row is left spinning forever.
    3. TEST 10 — From a clean state (no stuck row), drop an image, let it finish, then drop the SAME image
       again. Expected: a new result row appears AND a second file 'name-compressed-1.ext' is written
       beside the original (the original is never overwritten).
    4. TEST 6 — Drop a JPEG, let it finish, then move the quality slider (or pick a different preset).
       Expected: a "Re-compress at {n}%" button appears; clicking it re-compresses the existing image at
       the new quality and updates the result. For an all-PNG/TIFF batch where only quality changed, the
       button stays hidden (quality doesn't apply to lossless).

    After verifying, update .planning/phases/05-add-image-compression-feature/05-UAT.md: set Tests 5, 6,
    9, 10 result to 'pass' (or record any remaining issue), and refresh the Summary counts.
  </how-to-verify>
  <resume-signal>Type "approved" if all four re-pass, or describe any test that still fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact → human tester | a stale binary can mask a real fix or a real regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09A | Spoofing | stale DerivedData binary impersonates the fixed build | mitigate | clean build + launch by full path from recorded BUILD_DIR (MEMORY 'Stale DerivedData builds') |
| T-05-09-SC | Tampering | npm/pip/cargo installs | n/a | no package installs |
</threat_model>

<verification>
- Clean build + full FlintTests suite green (automated gate, Task 1).
- Human re-test confirms Tests 5, 6, 9, 10 pass on the freshly-built binary.
- 05-UAT.md updated with new results and Summary counts.
</verification>

<success_criteria>
- All four previously-failing UAT tests re-pass on a clean build.
- 05-UAT.md reflects the closure.
</success_criteria>

<output>
Create `.planning/phases/05-add-image-compression-feature/05-09-SUMMARY.md` when done.
</output>
