---
phase: 05-add-image-compression-feature
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - Tools/ImageCompress/ImageCompressTransformer.swift
  - Flint.xcodeproj/project.pbxproj
  - FlintTests/ImageCompressTransformerTests.swift
autonomous: true
gap_closure: true
requirements: [D-02, D-05, D-06, INFRA-17, INFRA-18]
must_haves:
  truths:
    - "Compressing a photographic PNG achieves savings comparable to dedicated PNG optimizers (~70-85%)"
    - "PNG quantization is ON by default and preserves alpha transparency"
    - "A PNG that would grow under quantization keeps the smaller output (honest reporting, D-06)"
    - "JPEG/HEIC behavior is unchanged; TIFF still re-encodes losslessly"
    - "Non-image / corrupt input still returns a typed failure, never crashes (INFRA-17)"
  artifacts:
    - path: "Tools/ImageCompress/ImageCompressTransformer.swift"
      provides: "PNG path that quantizes via PNGColorQuantizer + IndexedPNGEncoder, with truecolor fallback"
      contains: "PNGColorQuantizer"
    - path: "Flint.xcodeproj/project.pbxproj"
      provides: "Target membership for PNGColorQuantizer, IndexedPNGEncoder, PNGQuantizationTests"
      contains: "PNGColorQuantizer.swift"
  key_links:
    - from: "ImageCompressTransformer.compress"
      to: "PNGColorQuantizer.quantize + IndexedPNGEncoder.encode"
      via: "PNG branch replaces nil-props CGImageDestination path"
      pattern: "PNGColorQuantizer\\.quantize"
    - from: "Flint.xcodeproj/project.pbxproj"
      to: "app + test targets"
      via: "PBXBuildFile + PBXFileReference + group + Sources phase entries"
      pattern: "001200000007007|001200000007008|001200000007009"
---

<objective>
Wire the 05-04 quantization engine into ImageCompressTransformer's PNG path, register the three new files in the Xcode project, and prove the gap is closed: a photographic PNG now shrinks ~70-85% (UAT Test 8), alpha is preserved, and nothing regresses.

The current PNG path (lines 76-78) gives `nil` destination props, so PNGs are losslessly re-encoded as truecolor RGBA with no size win. This plan replaces that branch with: decode -> quantize -> indexed-PNG encode, while keeping the JPEG/HEIC quality path, the TIFF lossless path, the `-compressed` disambiguation, the CompressedImage/percentSaved size-delta logic, and the never-crash guarantee.

Purpose: Close UAT Test 8 — the only failing must-have — by emitting indexed-color PNGs Apple's ImageIO cannot produce.
Output: Updated transformer, updated pbxproj, new transformer-level PNG-savings tests, clean full build + full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-add-image-compression-feature/05-HUMAN-UAT.md

# The file to modify — PNG branch is lines 70-82, esp. props at 76-78:
@Tools/ImageCompress/ImageCompressTransformer.swift

# Consumers (read-only context — DO NOT change; they call compress(url:quality:) unchanged):
@Tools/ImageCompress/ImageCompressViewModel.swift
@Tools/ImageCompress/ImageCompressView.swift

# Existing transformer tests to extend (same @Suite, image-synthesis helpers):
@FlintTests/ImageCompressTransformerTests.swift

<interfaces>
<!-- Contracts produced by 05-04. The executor uses these directly; no exploration needed. -->

From Tools/ImageCompress/PNGColorQuantizer.swift:
  enum PNGColorQuantizer {
      struct QuantizedImage { let width: Int; let height: Int;
                              let palette: [(UInt8,UInt8,UInt8)]; let alpha: [UInt8]; let indices: [UInt8] }
      static func quantize(cgImage: CGImage, maxColors: Int = 256) -> QuantizedImage?
  }

From Tools/ImageCompress/IndexedPNGEncoder.swift:
  enum IndexedPNGEncoder {
      static func encode(width: Int, height: Int,
                         palette: [(UInt8,UInt8,UInt8)], alpha: [UInt8]?,
                         indices: [UInt8]) -> Data?
  }

(If 05-04's final signatures differ slightly, adapt the call sites to the actual signatures in those files — read them first.)
</interfaces>

<pbxproj_pattern>
<!-- Synthetic-ID convention already used for ImageCompress files. Phase-05 namespace = ...07xxx.
     Existing: ...07001..07006. Next free: 07007 (PNGColorQuantizer), 07008 (IndexedPNGEncoder), 07009 (PNGQuantizationTests).
     Four insertion sites mirror every existing ImageCompress file: -->

1. PBXBuildFile section (near line 137):
   001100000007007 /* PNGColorQuantizer.swift in Sources */ = {isa = PBXBuildFile; fileRef = 001200000007007 /* PNGColorQuantizer.swift */; };
   001100000007008 /* IndexedPNGEncoder.swift in Sources */ = {isa = PBXBuildFile; fileRef = 001200000007008 /* IndexedPNGEncoder.swift */; };
   001100000007009 /* PNGQuantizationTests.swift in Sources */ = {isa = PBXBuildFile; fileRef = 001200000007009 /* PNGQuantizationTests.swift */; };

2. PBXFileReference section (near line 264):
   001200000007007 /* PNGColorQuantizer.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = PNGColorQuantizer.swift; sourceTree = "<group>"; };
   001200000007008 /* IndexedPNGEncoder.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = IndexedPNGEncoder.swift; sourceTree = "<group>"; };
   001200000007009 /* PNGQuantizationTests.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = PNGQuantizationTests.swift; sourceTree = "<group>"; };

3. ImageCompress PBXGroup (near line 388-395): add 07007 and 07008 alongside the other 4 source files.

4. App Sources build phase (near line 842): add 07007 + 07008 .swift entries.
   Test Sources build phase (near line 870): add 07009 PNGQuantizationTests entry.
</pbxproj_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three new files in Flint.xcodeproj</name>
  <files>Flint.xcodeproj/project.pbxproj</files>
  <action>
    Add target membership for the three files created in 05-04 (PNGColorQuantizer.swift, IndexedPNGEncoder.swift in the app target; PNGQuantizationTests.swift in the test target), following the synthetic-ID pattern documented in the <pbxproj_pattern> context block above. Use IDs 07007 (quantizer), 07008 (encoder), 07009 (tests).

    Make four edits, each mirroring an existing ImageCompress entry exactly (same indentation/tabs):
    1. PBXBuildFile: add three lines after the existing ImageCompressDefinition build-file line (~line 137).
    2. PBXFileReference: add three lines after the existing ImageCompressDefinition file-ref line (~line 264).
    3. ImageCompress PBXGroup children: add 001200000007007 and 001200000007008 (NOT the test file — tests live in the FlintTests group, but the test build-phase entry in step 4 is what actually matters; mirror how ImageCompressViewModelTests is grouped — it is referenced in the test Sources phase but the existing ImageCompress group only lists app sources, so add only the two app files here).
    4. App Sources build phase (~line 842): add the two app .swift "in Sources" lines after ImageCompressView. Test Sources build phase (~line 870): add the PNGQuantizationTests "in Sources" line after ImageCompressViewModelTests.

    Use Edit with enough surrounding context to anchor each insertion uniquely. Do not renumber or touch any other IDs.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && plutil -lint Flint.xcodeproj/project.pbxproj && grep -c "001200000007007\|001200000007008\|001200000007009" Flint.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>pbxproj lints clean; the three new synthetic IDs appear (PNGColorQuantizer 2x, IndexedPNGEncoder 2x, PNGQuantizationTests 2x = 6 reference lines minimum across build-file + file-ref + phase entries).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the PNG path in ImageCompressTransformer with quantize+encode</name>
  <files>Tools/ImageCompress/ImageCompressTransformer.swift, FlintTests/ImageCompressTransformerTests.swift</files>
  <behavior>
    - Compressing a synthesised photographic-style PNG (large gradient, e.g. 256x256+ with many colors) produces a -compressed.png whose byte size is substantially smaller than the original (assert percentSaved is meaningfully positive — choose a threshold the quantizer comfortably clears, e.g. > 30% on a smooth gradient; the real-world photographic target is ~70-85%).
    - The compressed output is still a valid public.png openable by ImageIO with the SAME dimensions as the source (D-02 same-format-out, no downscaling).
    - A PNG containing transparency keeps an alpha channel after compression (decode the output and assert alpha is present / a known transparent region stays transparent).
    - A PNG with very few colors that would NOT shrink under quantization still returns success and never produces a file LARGER than the truecolor re-encode — i.e. the transformer keeps whichever output is smaller (D-06 honest reporting).
    - Existing tests stay green: JPEG round-trip unchanged, corrupt/empty -> .failure, disambiguation unchanged.
    - Non-image/corrupt PNG-extension input still returns .failure without crashing (INFRA-17).
  </behavior>
  <action>
    Modify `ImageCompressTransformer.compress(url:quality:)`. Keep steps 1-5 (source gate, UTI read, frame count, disambiguated destURL) and the lossy-quality branch for JPEG/HEIC unchanged. Replace ONLY the PNG handling.

    Branch on the source UTI: if it is PNG (`utType?.conforms(to: .png) == true`), take the new quantization path; otherwise keep the existing CGImageDestinationAddImageFromSource path (covers JPEG/HEIC lossy props and TIFF/other lossless nil props exactly as today).

    New PNG path:
    1. Create the source CGImage: `CGImageSourceCreateImageAtIndex(src, 0, nil)`. If nil -> fall through to the existing truecolor re-encode path (never fail outright; INFRA-17).
    2. `PNGColorQuantizer.quantize(cgImage:)` (default maxColors 256). If nil -> fall through to truecolor re-encode.
    3. `IndexedPNGEncoder.encode(...)` with the quantized palette/alpha/indices. If nil -> fall through to truecolor re-encode.
    4. Write the encoded Data to destURL. Then compute the truecolor-re-encode size as a guard: D-06 honest reporting means the user must never get a file bigger than a plain re-encode. Simplest correct approach: if the quantized Data byte count is NOT smaller than the original source file size, ALSO produce the plain truecolor re-encode (existing path) and keep whichever of {quantized, truecolor} is smaller; write that one to destURL. (For the common photographic case the quantized output wins outright and the truecolor path is skipped.) Keep this comparison cheap — compare in-memory Data sizes / file sizes, do not Data(contentsOf:) huge files unnecessarily.
    5. Preserve the rest: clean up partial writes on failure, compute origBytes/newBytes via resourceValues fileSizeKey, return `.success(CompressedImage(...))`. NEVER throw across the boundary (INFRA-17).

    Update the file's header comment (lines 4-7) so the PNG-path description reflects quantization rather than "PNG/TIFF get nil props". Keep TIFF described as lossless nil-props re-encode (TIFF path unchanged per locked constraint).

    Add the transformer-level PNG tests from <behavior> to `FlintTests/ImageCompressTransformerTests.swift` in the existing `@Suite("ImageCompressTransformer")`. Add a helper that synthesises a many-color gradient PNG (reuse/extend the existing writeTinyPNG helper into a `writeGradientPNG(width:height:)` that draws a smooth gradient via CGContext so it has hundreds of distinct colors). Add a helper that synthesises a PNG with a transparent quadrant. Assert percentSaved threshold, same dimensions, alpha preserved, and that the existing JPEG/corrupt/empty/disambiguation tests still pass untouched.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -only-testing:FlintTests/ImageCompressTransformer -only-testing:FlintTests/PNGQuantization 2>&1 | tail -40</automated>
  </verify>
  <done>The PNG path produces a smaller, valid, same-dimension indexed PNG with alpha preserved; the never-larger-than-truecolor guard holds (D-06); all pre-existing ImageCompressTransformer tests and the 05-04 PNGQuantization tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full build + full test suite green</name>
  <files>Tools/ImageCompress/ImageCompressTransformer.swift</files>
  <action>
    Run the complete app build and the entire test suite to confirm the new files compile into both targets, the wiring is correct, and nothing in the rest of the app regressed. Fix any compile errors surfaced by the real target build (the in-isolation checks in 05-04 may not have caught target-membership or Swift-6 concurrency issues until now). Do not introduce new public API beyond what the consumers (ViewModel/View) already call — they must keep calling `compress(url:quality:)` unchanged.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -5 && xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -15</automated>
  </verify>
  <done>`xcodebuild build` succeeds (BUILD SUCCEEDED) and the full `xcodebuild test` run reports all suites passing (TEST SUCCEEDED), including the new PNGQuantization and extended ImageCompressTransformer tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dropped file -> compress() | Untrusted image bytes enter via CGImageSource; already gated. The new PNG path decodes to CGImage and quantizes — same untrusted-input surface, no new external boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Denial of Service | compress() on a huge PNG | mitigate | Already runs off-main inside autoreleasepool per image (INFRA-18, ViewModel unchanged). Quantize/encode are linear in pixels; no recursion blowup. |
| T-05-05-02 | Tampering | quantize/encode returning nil mid-path | mitigate | Every nil result falls through to the existing truecolor re-encode; compress() still returns a typed Result, never throws (INFRA-17). |
| T-05-05-03 | Denial of Service | Output larger than input | mitigate | D-06 never-larger guard: keep min(quantized, truecolor) so the user is never handed a bigger file. |
| T-05-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure Swift, Foundation + Compression + CoreGraphics only. No supply-chain surface. |
</threat_model>

<verification>
- pbxproj lints and the three new files build into the correct targets.
- A photographic-style PNG compresses with meaningful, positive savings; output is a valid same-dimension public.png.
- Alpha transparency survives quantization.
- D-06 honest reporting holds: output is never larger than the truecolor re-encode.
- JPEG/HEIC/TIFF paths and all prior tests are unchanged and green.
- Corrupt/non-image input returns .failure without crashing (INFRA-17).
- Full `xcodebuild build` + `xcodebuild test` succeed.
</verification>

<success_criteria>
- UAT Test 8 gap closed: compressing a photographic PNG achieves savings comparable to dedicated PNG optimizers, fully offline with zero external dependencies, preserving alpha and never overwriting the source.
- No regression to any other tool or to the JPEG/HEIC/TIFF compression paths.
</success_criteria>

<output>
Create `.planning/phases/05-add-image-compression-feature/05-05-SUMMARY.md` when done.
</output>
