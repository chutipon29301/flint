---
phase: 02-extended-tools
plan: 04
type: execute
wave: 4
depends_on: ["02-01"]
files_modified:
  - Tools/Markdown/MarkdownTransformer.swift
  - Tools/Markdown/MarkdownViewModel.swift
  - Tools/Markdown/MarkdownView.swift
  - Tools/Markdown/MarkdownDefinition.swift
  - UI/Components/WebPreviewView.swift
  - FlintTests/MarkdownTransformerTests.swift
  - Flint.xcodeproj/project.pbxproj
autonomous: false
requirements: [MD-01, MD-02, MD-03, MD-04]
user_setup: []

must_haves:
  truths:
    - "User writes Markdown and sees a live GFM preview (tables, task lists, fenced code, strikethrough), debounced"
    - "Editor highlights Markdown syntax; preview highlights fenced code blocks"
    - "User can export as copied HTML, a saved .html file, or a saved .pdf"
    - "A status footer shows word count and reading-time; a toolbar inserts bold/italic/link/image/code/table"
  artifacts:
    - path: "Tools/Markdown/MarkdownTransformer.swift"
      provides: "swift-markdown AST → escaped GFM HTML; word-count + reading-time pure fns"
      contains: "enum MarkdownTransformer"
    - path: "UI/Components/WebPreviewView.swift"
      provides: "NSViewRepresentable WKWebView: lazy, JS-off, nav-blocked, identical-HTML guard, createPDF"
      contains: "WebPreviewView"
    - path: "Tools/Markdown/MarkdownViewModel.swift"
      provides: "Debounced render; HTML output; ToolShortcutActions"
      contains: "MarkdownViewModel"
    - path: "Tools/Markdown/MarkdownView.swift"
      provides: "Toggle in popover / split in window; toolbar; footer; export"
      contains: "MarkdownView"
    - path: "FlintTests/MarkdownTransformerTests.swift"
      provides: "AST→HTML for tables/task-lists/strikethrough/code; HTML escaping; word-count; reading-time"
      contains: "MarkdownTransformer"
  key_links:
    - from: "Tools/Markdown/MarkdownTransformer.swift"
      to: "Markdown (swift-markdown)"
      via: "Document(parsing:) + MarkupVisitor"
      pattern: "Document\\(parsing"
    - from: "Tools/Markdown/MarkdownView.swift"
      to: "WebPreviewView"
      via: "preview pane"
      pattern: "WebPreviewView"
    - from: "Tools/Markdown/MarkdownTransformer.swift"
      to: "Resources/github-markdown.css"
      via: "inlined into <head><style>"
      pattern: "github-markdown"
---

<objective>
Deliver the Markdown Previewer as a complete vertical slice: swift-markdown AST→HTML transformer + new WKWebView component (`WebPreviewView`) + debounced ViewModel + toggle/split View with toolbar/footer/export + transformer tests.

Purpose: MD-01..04 — live GFM preview, dual syntax highlighting, HTML/.html/.pdf export, word-count/reading-time + formatting toolbar. The WKWebView (JS-off, nav-blocked, baseURL:nil, identical-HTML guard, lazy init) plus inlined GitHub CSS are the new-component risk; A1 (loadHTMLString on non-sandboxed macOS 14) must be verified early via a human checkpoint.
Output: A demoable Markdown tool (registration is the Wave-7 plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extended-tools/02-RESEARCH.md
@.planning/phases/02-extended-tools/02-PATTERNS.md
@.planning/phases/02-extended-tools/02-UI-SPEC.md

<interfaces>
From Core/Models/ToolDefinition.swift: makeView: @MainActor () -> AnyView; detectionPredicate may be nil.
From Core/Models/HistoryEntry.swift: init(tool:input:output:timestamp:pinned:)
From UI/Components/ToolShortcutActions.swift: protocol with primaryOutput()->String?, clearInput(); view .toolShortcuts(vm)
Project-wide `actor Debounce` (in JSONFormatterViewModel.swift) — import, do NOT redefine.

swift-markdown (product "Markdown", added 02-01) — verified visit methods:
  Markdown.Document(parsing: String) -> Document
  protocol MarkupVisitor (Result = String): visitHeading/visitParagraph/visitCodeBlock/visitBlockQuote/
    visitThematicBreak/visitTable/visitTableHead/visitTableBody/visitTableRow/visitTableCell (Table.ColumnAlignment .left/.center/.right)/
    visitStrikethrough/visitListItem (.checkbox: Checkbox? .checked/.unchecked → task list)/visitOrderedList/visitUnorderedList/
    visitLink/visitImage/visitEmphasis/visitStrong/visitInlineCode/visitText

WKWebView: loadHTMLString(_ string: String, baseURL: URL?); createPDF(configuration:completionHandler:) -> Result<Data,Error>
  WKWebViewConfiguration.defaultWebpagePreferences.allowsContentJavaScript (set false)
Resources/github-markdown.css bundled in 02-01: Bundle.main.url(forResource:"github-markdown", withExtension:"css")

Reusable: SyntaxEditorView(text:accessibilityLabel:), CodeDisplayView(code:language:), MainWindowView (HSplitView roomy),
  HashView NSSavePanel pattern (lines 202-213).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarkdownTransformer (AST→escaped HTML) + MarkdownTransformerTests</name>
  <files>Tools/Markdown/MarkdownTransformer.swift, FlintTests/MarkdownTransformerTests.swift</files>
  <read_first>
    - Tools/JSONFormatter/JSONTransformer.swift (enum + Result + guards analog)
    - FlintTests/JSONTransformerTests.swift (XCTest analog)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §5 (Document(parsing:), the verified MarkupVisitor method list, task-list checkbox, table alignment, XSS-escaping rule, word-count/reading-time)
    - .planning/phases/02-extended-tools/02-PATTERNS.md per-tool MarkdownTransformer note
  </read_first>
  <behavior>
    - Heading "# Hi" → `<h1>Hi</h1>`.
    - GFM table (`| a | b |\n|---|---|\n| 1 | 2 |`) → `<table>` with `<th>` and `<td>` cells; column alignment maps to align attributes.
    - Task list (`- [x] done\n- [ ] todo`) → `<input type="checkbox" checked>` / unchecked.
    - Strikethrough (`~~x~~`) → `<del>x</del>` (or `<s>`).
    - Fenced code (```` ```swift\nlet x=1\n``` ````) → a `<pre><code>` block with inline-styled spans (no JS).
    - XSS escaping: input text node `<script>alert(1)</script>` is HTML-escaped (emitted as `&lt;script&gt;…`), NOT raw.
    - wordCount("one two three") == 3; readingTime(words: 400) == ceil(400/200) == 2.
    - Empty / huge input → graceful (no crash, INFRA-17).
  </behavior>
  <action>Create `enum MarkdownTransformer` (`import Foundation` + `import Markdown`; no SwiftUI). Implement `static func renderHTML(_ source: String) -> Result<String, TransformError>` parsing via `Markdown.Document(parsing: source)` and walking it with a custom struct conforming to `MarkupVisitor` (Result == String) that emits CONTROLLED, HTML-ESCAPED HTML — every text node escaped via a helper (`&`,`<`,`>`,`"` → entities); NEVER emit raw user HTML (XSS hardening). Cover the verified visit methods: headings, paragraphs, code blocks (emit `<pre><code>` with pre-rendered inline-styled spans so no JS/highlight.js is needed — keeps it offline), block quotes, thematic breaks, GFM tables (use `Table.ColumnAlignment` → th/td align), strikethrough (`<del>`), list items with `.checkbox` (task-list `<input type=checkbox [checked]>`), ordered/unordered lists, links, images, emphasis/strong/inline-code, text. Provide `static func fullStyledHTML(_ source: String) -> Result<String, TransformError>` that wraps the rendered body in `<html><head><style>…</style></head><body>…</body></html>` with the bundled `Resources/github-markdown.css` contents INLINED into the `<style>` block (read via `Bundle.main.url(forResource:"github-markdown", withExtension:"css")`) — fully self-contained, no external loads. Also `static func wordCount(_ s: String) -> Int` (whitespace-split token count) and `static func readingTimeMinutes(words: Int) -> Int` (`ceil(words/200.0)`, min 1). Add a size guard (INFRA-17). Write `FlintTests/MarkdownTransformerTests.swift` (XCTest) covering every behavior above — especially the XSS-escaping assertion and the word-count/reading-time pure fns.</action>
  <verify>
    <automated>grep -q "Document(parsing" Tools/Markdown/MarkdownTransformer.swift && ! grep -q "import SwiftUI" Tools/Markdown/MarkdownTransformer.swift && xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -only-testing:FlintTests/MarkdownTransformerTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `MarkdownTransformer` parses via `Markdown.Document(parsing:)` and uses a `MarkupVisitor` returning String.
    - Tables, task lists, strikethrough, and fenced code render to the correct GFM HTML (asserted in tests).
    - Text-node content is HTML-escaped — the `<script>` test proves no raw HTML passthrough.
    - `fullStyledHTML` inlines the bundled CSS (contains the stylesheet text, no external `<link>`).
    - wordCount and readingTimeMinutes are pure and tested.
    - `xcodebuild test ... -only-testing:FlintTests/MarkdownTransformerTests` passes (exit 0), incl. empty/huge input.
  </acceptance_criteria>
  <done>GFM Markdown renders to self-contained, escaped, GitHub-styled HTML; word-count/reading-time proven.</done>
</task>

<task type="auto">
  <name>Task 2: WebPreviewView (WKWebView NSViewRepresentable) — lazy, JS-off, nav-blocked, identical-HTML guard, createPDF</name>
  <files>UI/Components/WebPreviewView.swift, Flint.xcodeproj/project.pbxproj</files>
  <read_first>
    - UI/Components/SyntaxEditorView.swift (NSViewRepresentable + the re-render guard, lines 9-50 — "Pitfall #5 in different clothing")
    - .planning/phases/02-extended-tools/02-RESEARCH.md §5 (defer instantiation; allowsContentJavaScript=false; block navigation; baseURL:nil; createPDF only after navigation-finished; A1 entitlement note — do NOT add network key)
    - .planning/phases/02-extended-tools/02-PATTERNS.md "WebPreviewView" section
    - .planning/phases/02-extended-tools/02-UI-SPEC.md "Tool 3: Markdown Previewer" Preview interaction bullet
  </read_first>
  <action>Create `struct WebPreviewView: NSViewRepresentable` (`import SwiftUI` + `import WebKit`) wrapping a `WKWebView`. `makeNSView`: build `WKWebViewConfiguration` with `defaultWebpagePreferences.allowsContentJavaScript = false`; set a `WKNavigationDelegate` (the Coordinator) whose `decidePolicyFor` returns `.cancel` for any navigation action whose `navigationType != .other` (blocks all link/form navigation; allows the initial loadHTMLString). Take an `html: String` input. `updateNSView`: GUARD `guard html != context.coordinator.lastLoadedHTML else { return }` then `loadHTMLString(html, baseURL: nil)` and store lastLoadedHTML (Pitfall #5 in WKWebView clothing — prevents reload loops). Expose a way to export PDF: a method/closure `exportPDF(to url: URL)` that calls `webView.createPDF(configuration:)` ONLY after the navigation-finished delegate fired (track a `didFinishNavigation` flag in the Coordinator; if not finished, defer until it is) and writes the `Result<Data,Error>` data to the URL. Defer WKWebView creation to first appearance (do not instantiate at app startup — cold-start budget, Pitfall #6). Add `.accessibilityLabel("Markdown preview")`. Add the file to the Flint target in project.pbxproj.</action>
  <verify>
    <automated>grep -q "allowsContentJavaScript = false" UI/Components/WebPreviewView.swift && grep -q "lastLoadedHTML" UI/Components/WebPreviewView.swift && grep -q "baseURL: nil" UI/Components/WebPreviewView.swift && grep -q "createPDF" UI/Components/WebPreviewView.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `WebPreviewView` is an `NSViewRepresentable` over `WKWebView` with `allowsContentJavaScript = false`.
    - Navigation is blocked (`decidePolicyFor → .cancel` for non-`.other` actions).
    - `updateNSView` guards identical HTML before `loadHTMLString(html, baseURL: nil)`.
    - A PDF export path uses `createPDF` and only after navigation-finished.
    - No network entitlement is added (verify `Flint.entitlements`/build settings unchanged for network keys).
    - Project builds (exit 0).
  </acceptance_criteria>
  <done>A safe, offline, leak-free WKWebView preview component with PDF export, reusable beyond Markdown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify WKWebView renders local HTML on macOS 14 (Assumption A1)</name>
  <what-built>Task 1 produced self-contained styled HTML; Task 2 produced the JS-off, nav-blocked WebPreviewView. RESEARCH Assumption A1 (MEDIUM risk) is that a non-sandboxed Flint can `loadHTMLString` local content WITHOUT a network entitlement. This checkpoint confirms it before the View wiring depends on it. If blank, the fix is to confirm the WebKit content process launches — NOT to add the network entitlement (violates the project's no-network constraint).</what-built>
  <how-to-verify>
    1. Run the app (`xcodebuild build` then launch the .app, or run the Flint scheme).
    2. Temporarily host `WebPreviewView(html: MarkdownTransformer.fullStyledHTML("# Hello\n\n| a | b |\n|---|---|\n| 1 | 2 |\n\n- [x] done").get())` in a preview/test harness or the Markdown view once Task 4 lands.
    3. Confirm: the heading, a styled table, and a checked task-list item render (not blank), and Light/Dark CSS adapts.
  </how-to-verify>
  <resume-signal>Type "approved" if the preview renders, or describe what is blank/broken (do NOT add a network entitlement).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: MarkdownViewModel (debounced) + MarkdownView (toggle/split, toolbar, footer, export) + MarkdownDefinition</name>
  <files>Tools/Markdown/MarkdownViewModel.swift, Tools/Markdown/MarkdownView.swift, Tools/Markdown/MarkdownDefinition.swift, Flint.xcodeproj/project.pbxproj</files>
  <read_first>
    - Tools/JSONFormatter/JSONFormatterViewModel.swift (Debounce reuse + last-good + injected onSaveHistory + ToolShortcutActions)
    - Tools/JSONFormatter/JSONFormatterView.swift (HSplitView roomy layout; @Bindable content view; .toolShortcuts)
    - Tools/Hash/HashDefinition.swift (Definition WITHOUT predicate + HashViewWrapper history injection)
    - Tools/Hash/HashView.swift (NSSavePanel export pattern, lines 202-213)
    - .planning/phases/02-extended-tools/02-UI-SPEC.md "Tool 3: Markdown Previewer" (popover toggle vs window split diagrams, toolbar SF Symbols + insert actions, footer copy, export menu, all accessibility labels, popover-vs-window threshold)
  </read_first>
  <action>Create `@Observable @MainActor final class MarkdownViewModel: ToolShortcutActions`: `source: String { didSet { scheduleRender() } }`, `html: String`, `wordCountText`, `readingTimeText`, `outputDimmed`, `errorMessage`. `scheduleRender()` uses the existing `Debounce` actor at ~300ms then calls `MarkdownTransformer.fullStyledHTML(source)`; on success set html + recompute word-count/reading-time via the transformer pure fns + write a HistoryEntry (`tool: "markdown"`, input = source, output = html); on failure keep last-good html dimmed (CF-02). Inject `onSaveHistory`; no GRDB. Implement `primaryOutput()` (html) and `clearInput()`. Create `MarkdownView`: detect popover-vs-window width (GeometryReader or NSApp.keyWindow frame; threshold ~600pt per UI-SPEC) — in popover show a `Picker(.segmented)` "Editor"/"Preview" toggling one pane; in the window show a true `HSplitView` editor|preview. Editor = `SyntaxEditorView(text: $vm.source, accessibilityLabel: "Markdown editor")` with a Markdown-syntax attribute-only highlight pass (re-entrancy guarded, never reset `.string` — Pitfall #5). Preview = `WebPreviewView(html: vm.html)`. Above the editor: a formatting toolbar (B/I/link/image/inline-code/code-block/table) — each `Button` mutates the source binding around the current selection (pure string ops: bold→`**…**`, italic→`*…*`, link→`[text](url)`, image→`![alt](url)`, inline code→`` `…` ``, code block→fenced, table→2×2 GFM template), each with the UI-SPEC tooltip + `.accessibilityLabel`. Bottom: a thin footer `HStack` with `.caption` "words: N  •  ~M min read", a "Copy HTML" `Button` (copies vm.html to pasteboard), and a "Save ▾" `Menu` with "Save as HTML…" (NSSavePanel `.html`, write vm.html) and "Save as PDF…" (NSSavePanel `.pdf`, call the WebPreviewView PDF export AFTER navigation-finished). Apply `.toolShortcuts(viewModel)`. Create `enum MarkdownDefinition { static func make() -> ToolDefinition }` id "markdown", name "Markdown Previewer", category `.formatting`, keywords ["markdown","md","gfm","preview","html"], sfSymbol "doc.richtext", `detectionPredicate: nil` (search-only), `makeView: { @MainActor in AnyView(MarkdownViewWrapper()) }` with a wrapper injecting history (Hash convention). Do NOT register in ToolRegistry. Add the three files to project.pbxproj.</action>
  <verify>
    <automated>grep -q "WebPreviewView" Tools/Markdown/MarkdownView.swift && grep -q "category: .formatting" Tools/Markdown/MarkdownDefinition.swift && grep -q "detectionPredicate: nil" Tools/Markdown/MarkdownDefinition.swift && grep -q "NSSavePanel" Tools/Markdown/MarkdownView.swift && grep -q "ToolShortcutActions" Tools/Markdown/MarkdownViewModel.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `MarkdownView` shows a segmented Editor/Preview toggle in the narrow popover and a side-by-side `HSplitView` in the window.
    - The formatting toolbar's seven buttons insert the correct Markdown syntax around the selection; each has an `.accessibilityLabel` and the UI-SPEC tooltip.
    - The footer shows "words: N" and "~M min read"; export offers Copy HTML, Save as HTML…, Save as PDF… (PDF after navigation-finished).
    - `MarkdownDefinition.make()` returns id "markdown", category `.formatting`, `detectionPredicate: nil`.
    - Render is debounced via the existing Debounce actor; `.toolShortcuts(viewModel)` applied.
    - Project builds (exit 0); MarkdownDefinition.make() NOT yet appended to ToolRegistry.tools.
  </acceptance_criteria>
  <done>A complete Markdown tool: debounced live GFM preview, dual highlighting, toolbar, footer, and HTML/.html/.pdf export, ready for Wave-7 registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user Markdown → rendered HTML in WKWebView | Untrusted Markdown could inject script/handlers into the preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-MD-XSS | Tampering / EoP | MarkdownTransformer → WebPreviewView | mitigate | AST→escaped HTML (text nodes entity-escaped, no raw passthrough); WKWebView allowsContentJavaScript=false; navigation blocked; baseURL:nil; CSS inlined (no remote loads) |
| T-02-MD-NET | Information Disclosure | WKWebView local-content load | accept/verify | Non-sandboxed, no network entitlement; loadHTMLString of local content only — verified in Task 3 (A1); never add network key |
| T-02-MD-IV | Input Validation | MarkdownTransformer | mitigate | Size guard + empty/huge tested; never force-unwrap (INFRA-17) |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:FlintTests/MarkdownTransformerTests` passes (incl. XSS-escaping assertion).
- `xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS'` succeeds.
- Task 3 human checkpoint: WKWebView renders local styled HTML on macOS 14 without a network entitlement.
- MANUAL/UAT: type GFM, see live debounced preview; export PDF produces a non-blank, styled PDF.
</verification>

<success_criteria>
MD-01..04 met: live debounced GFM preview (tables/task-lists/fenced-code/strikethrough); editor + code-block highlighting; copy/.html/.pdf export; word-count + reading-time + formatting toolbar. Tool built and ready to register.
</success_criteria>

<output>
Create `.planning/phases/02-extended-tools/02-04-SUMMARY.md` when done.
</output>
