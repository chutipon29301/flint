---
phase: 02-extended-tools
plan: 07
type: execute
wave: 7
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - Core/Services/ToolRegistry.swift
autonomous: false
requirements: [RGX-02, CLR-02]
user_setup: []

must_haves:
  truths:
    - "All five new tools appear in the launcher and are reachable via global fuzzy search"
    - "Copying a hex color (#RRGGBB) or a /…/ regex literal fires the detection banner for the matching tool, without shadowing the existing JSON→JWT→Base64→URL→Timestamp→UUID order"
    - "Every new tool records to history and responds to ⌘⇧C / ⌘⌫ shortcuts"
  artifacts:
    - path: "Core/Services/ToolRegistry.swift"
      provides: "tools array with the five Phase-2 make() calls appended (the one allowed frozen-file edit)"
      contains: "RegexDefinition.make()"
  key_links:
    - from: "Core/Services/ToolRegistry.swift"
      to: "RegexDefinition / ColorDefinition / MarkdownDefinition / NumberBaseDefinition / TextDiffDefinition"
      via: "tools array append"
      pattern: "(Regex|Color|Markdown|NumberBase|TextDiff)Definition.make\\(\\)"
---

<objective>
Integrate all five Phase-2 tools into the application by appending their `make()` calls to `ToolRegistry.tools` — the single, explicitly-sanctioned edit to the FROZEN ToolRegistry — and verify end-to-end reachability (launcher, search, detection, history, shortcuts) across all five.

Purpose: The five tool quads (Plans 02-06) build but are not yet reachable; this wave wires them in and proves the toolkit is complete. Per RESEARCH §0 and PATTERNS, the freeze intent is "don't reshape the struct," NOT "never append" — appending five lines is the one allowed mutation. Detection-predicate ordering (Color hex early, Regex conservative/nil) must not shadow the existing chain.
Output: A complete, reachable 12-tool Flint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extended-tools/02-RESEARCH.md
@.planning/phases/02-extended-tools/02-PATTERNS.md

<interfaces>
From Core/Services/ToolRegistry.swift (current — append only, do NOT reshape):
  tools = [
    JSONFormatterDefinition.make(), Base64Definition.make(), URLEncoderDefinition.make(),
    JWTDefinition.make(), TimestampDefinition.make(), HashDefinition.make(), UUIDDefinition.make(),
  ]
  func detect(from string: String) -> DetectionResult?  // first-match-wins, follows array order

New definitions (built in Plans 02-06): RegexDefinition.make() (predicate: conservative /…/ or nil),
  ColorDefinition.make() (predicate: narrow hex #RRGGBB), MarkdownDefinition.make() (nil),
  NumberBaseDefinition.make() (nil), TextDiffDefinition.make() (nil).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the five Phase-2 make() calls to ToolRegistry.tools</name>
  <files>Core/Services/ToolRegistry.swift</files>
  <read_first>
    - Core/Services/ToolRegistry.swift (the existing tools array lines 15-23 and the detect(from:) loop lines 38-43 — the ONLY allowed edit is appending to the array)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §0 (FROZEN-file append is sanctioned; detection-chain ordering rule; A5)
    - .planning/phases/02-extended-tools/02-PATTERNS.md "Core/Services/ToolRegistry.swift (MODIFY)" (the exact append snippet + ordering note)
    - .planning/phases/02-extended-tools/02-04-SUMMARY.md, 02-02-SUMMARY.md (confirm the actual chosen Regex predicate decision and Color predicate so ordering is correct)
  </read_first>
  <action>Append exactly five entries to the `tools` array in `Core/Services/ToolRegistry.swift`, after the seven Phase-1 entries: `RegexDefinition.make()`, `ColorDefinition.make()`, `MarkdownDefinition.make()`, `NumberBaseDefinition.make()`, `TextDiffDefinition.make()`. Do NOT reshape the struct, the init, or the `detect(from:)` loop. ORDER THEM FOR DETECTION CORRECTNESS (first-match-wins follows array order): place `ColorDefinition.make()` (narrow hex `#RRGGBB`) so its predicate runs without shadowing the existing JSON→JWT→Base64→URL→Timestamp→UUID matches — hex is highly specific, so placing it among the new entries is safe (it cannot false-positive on JSON/Base64/URL). If Plan 02-02 chose a Regex `/…/` predicate, ensure it is conservative and ordered so it never hijacks generic input; if Plan 02-02 chose `nil` (search-only), no ordering concern. Add a one-line comment marking these as the sanctioned Phase-2 append (per RESEARCH §0 / A5) so the plan-checker does not reject the frozen-file edit.</action>
  <verify>
    <automated>grep -q "RegexDefinition.make()" Core/Services/ToolRegistry.swift && grep -q "ColorDefinition.make()" Core/Services/ToolRegistry.swift && grep -q "MarkdownDefinition.make()" Core/Services/ToolRegistry.swift && grep -q "NumberBaseDefinition.make()" Core/Services/ToolRegistry.swift && grep -q "TextDiffDefinition.make()" Core/Services/ToolRegistry.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All five `*Definition.make()` calls are present in `ToolRegistry.tools`.
    - The seven Phase-1 entries remain unchanged and the struct/init/`detect(from:)` loop are NOT reshaped.
    - A comment marks the append as the sanctioned Phase-2 edit (RESEARCH §0).
    - The project builds (exit 0) with all 12 tools registered.
  </acceptance_criteria>
  <done>All five Phase-2 tools are registered; the toolkit enumerates 12 tools.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green across the phase</name>
  <files>Core/Services/ToolRegistry.swift</files>
  <read_first>
    - FlintTests/ (all Phase-2 transformer tests + SwiftDiffVendorTests must exist by now)
  </read_first>
  <action>Run the complete FlintTests suite to confirm all Phase-2 transformer tests (Regex, Color, Markdown, NumberBase, TextDiff) and the SwiftDiffVendorTests pass alongside the nine Phase-1 transformer tests — no regressions from the registry edit. If any test fails, fix the integration-level cause (do not modify the frozen components; the likely cause is a registration ordering or a missing target membership). This task makes no source change unless a failure surfaces.</action>
  <verify>
    <automated>xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS'` reports TEST SUCCEEDED (exit 0).
    - All five Phase-2 *TransformerTests and SwiftDiffVendorTests are included and pass; no Phase-1 test regresses.
  </acceptance_criteria>
  <done>Entire FlintTests suite is green with all 12 tools registered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end reachability of all five tools (launcher, search, detection, history, shortcuts)</name>
  <what-built>Tasks 1-2 registered the five tools and confirmed all unit tests pass. This checkpoint verifies the end-to-end behaviors that cannot be unit-tested: launcher visibility, fuzzy search, clipboard detection banners, history recording, and the ⌘⇧C/⌘⌫ shortcuts — plus the two manual UAT items flagged across the phase (Regex never-freeze, Markdown PDF, OKLCH gamut).</what-built>
  <how-to-verify>
    1. Launch the app. Open the popover; confirm Regex Tester, Color Converter, Markdown Previewer, Number Base Converter, and Text Diff all appear in the launcher and via global fuzzy search (type "regex", "color", "markdown", "base", "diff").
    2. Detection: copy a hex color like `#3366FF` → the detection banner offers Color Converter. (If a /…/ regex predicate was implemented, copy `/\d+/g` and confirm Regex is offered; if search-only, skip.) Confirm copying JSON / a JWT / Base64 still routes to the Phase-1 tools (no shadowing).
    3. History: run a transform in each new tool; confirm each appears in the history panel and re-opens correctly.
    4. Shortcuts: in each new tool, ⌘⇧C copies output and ⌘⌫ clears input.
    5. UAT spot-checks: Regex pattern `(a+)+$` on `"aaaaaaaaaaaaaaaaaaaa!"` keeps the UI responsive with the timeout warning; Markdown "Save as PDF…" produces a non-blank styled PDF; a high-chroma OKLCH shows the out-of-gamut warning.
  </how-to-verify>
  <resume-signal>Type "approved" if all five tools are reachable and the UAT spot-checks pass, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard input → detection chain | Untrusted clipboard content matched against ordered predicates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-INT-ORDER | Tampering (mis-routing) | ToolRegistry.detect first-match-wins | mitigate | Color hex predicate is narrow; Regex predicate conservative or nil; ordered so Phase-1 routes are not shadowed (verified in Task 3) |
| T-02-INT-FREEZE | Tampering | Frozen ToolRegistry struct | accept | Only the sanctioned five-line array append (RESEARCH §0/A5); struct/init/detect loop untouched |
</threat_model>

<verification>
- `xcodebuild build` and full `xcodebuild test` both succeed.
- Task 3 human checkpoint confirms launcher/search/detection/history/shortcuts and the three UAT spot-checks.
</verification>

<success_criteria>
The toolkit is complete: all five Phase-2 tools are registered, reachable via launcher and search, detect correctly without shadowing Phase-1, record to history, and respond to the standard shortcuts. Phase-2 goal achieved (all 19 requirements demonstrably reachable end-to-end).
</success_criteria>

<output>
Create `.planning/phases/02-extended-tools/02-07-SUMMARY.md` when done.
</output>
