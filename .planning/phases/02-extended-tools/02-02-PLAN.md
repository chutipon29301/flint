---
phase: 02-extended-tools
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Tools/Regex/RegexTransformer.swift
  - Tools/Regex/RegexViewModel.swift
  - Tools/Regex/RegexView.swift
  - Tools/Regex/RegexDefinition.swift
  - FlintTests/RegexTransformerTests.swift
  - Flint.xcodeproj/project.pbxproj
autonomous: true
requirements: [RGX-01, RGX-02, RGX-03, RGX-04]
user_setup: []

must_haves:
  truths:
    - "User can enter a regex pattern with g/i/m/s/x flag toggles and a multi-line test string (D-01)"
    - "Matches highlight live in the test string color-coded per capture group with a match-count badge (D-03)"
    - "A results table shows index, position, full match, and named + numbered capture groups (D-03)"
    - "Replace mode previews substitution output and a Patterns menu inserts email/URL/phone/date/IP presets (D-04)"
    - "A catastrophic-backtracking pattern never freezes the UI — a timeout warning shows and the last-good highlight stays dimmed (D-02)"
  artifacts:
    - path: "Tools/Regex/RegexTransformer.swift"
      provides: "Pure matches(pattern:flags:in:) and substitute(pattern:flags:in:template:)"
      contains: "enum RegexTransformer"
    - path: "Tools/Regex/RegexViewModel.swift"
      provides: "Off-main eval + 300ms debounce + 2s timeout + cancel-in-flight; ToolShortcutActions"
      contains: "RegexViewModel"
    - path: "Tools/Regex/RegexView.swift"
      provides: "Vertical-stack UI: pattern+flags, test editor, results table, replace section"
      contains: "RegexView"
    - path: "Tools/Regex/RegexDefinition.swift"
      provides: "ToolDefinition with id regex, category .analysis"
      contains: "RegexDefinition"
    - path: "FlintTests/RegexTransformerTests.swift"
      provides: "Per-requirement transformer tests incl. garbage-input no-crash"
      contains: "RegexTransformer"
  key_links:
    - from: "Tools/Regex/RegexDefinition.swift"
      to: "RegexView"
      via: "makeView factory"
      pattern: "AnyView\\(.*RegexView"
    - from: "Tools/Regex/RegexViewModel.swift"
      to: "RegexTransformer"
      via: "transformer call inside timeout race"
      pattern: "RegexTransformer\\.(matches|substitute)"
---

<objective>
Deliver the Regex Tester as a complete vertical slice: pure transformer + @Observable ViewModel (with the never-freeze timeout that is the single hardest UX constraint of the phase) + vertical-stack View + registry-ready Definition + transformer tests.

Purpose: RGX-01..04 — pattern+flags, live color-coded match highlighting with a count badge, a capture-group results table, and replace mode with a pattern library. RGX-02's "never freeze on pathological patterns" is non-negotiable and lives in the ViewModel.
Output: A demoable Regex tool (not yet registered — registration is the Wave-7 integration plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extended-tools/02-RESEARCH.md
@.planning/phases/02-extended-tools/02-PATTERNS.md
@.planning/phases/02-extended-tools/02-UI-SPEC.md

<interfaces>
<!-- Frozen contracts the executor builds against. Extracted from repo — no exploration needed. -->

From Core/Models/ToolDefinition.swift:
  struct ToolDefinition: Identifiable, Sendable {
    let id: String; let name: String; let category: ToolCategory
    let keywords: [String]; let sfSymbol: String
    let detectionPredicate: (@Sendable (String) -> DetectionResult?)?
    let makeView: @MainActor () -> AnyView
  }

From Core/Models/HistoryEntry.swift:
  init(id: Int64? = nil, tool: String, input: String, output: String, timestamp: Date = Date(), pinned: Bool = false)

From UI/Components/ToolShortcutActions.swift (protocol every VM conforms to):
  @MainActor protocol ToolShortcutActions: AnyObject { func primaryOutput() -> String?; func clearInput() }
  // view applies: .toolShortcuts(viewModel)

From UI/Components/WarningBannerView.swift:
  enum BannerSeverity { case warning; case error }
  WarningBannerView(message: String, severity: BannerSeverity)

Reusable (FROZEN — do not modify): SyntaxEditorView(text: Binding<String>, accessibilityLabel: String),
  CodeDisplayView(code:language:), InlineErrorView(message: String?), CopyButtonView(getText:)/(text:),
  the project-wide `actor Debounce` (defined once in JSONFormatterViewModel.swift — import, do NOT redefine).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RegexTransformer (pure) + RegexTransformerTests</name>
  <files>Tools/Regex/RegexTransformer.swift, FlintTests/RegexTransformerTests.swift</files>
  <read_first>
    - Tools/JSONFormatter/JSONTransformer.swift (analog: enum + nested error + Result + input guards, lines 6-48)
    - FlintTests/JSONTransformerTests.swift (XCTest test analog)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §3 (NSRegularExpression engine, flag→Options mapping, RegexMatch shape, named/numbered groups)
    - .planning/phases/02-extended-tools/02-PATTERNS.md "All five *Transformer.swift" + per-tool RegexTransformer note
  </read_first>
  <behavior>
    - matches with a numbered group: pattern `(\d+)-(\d+)` on "12-34" returns one match with full-match range and two numbered group strings "12" and "34".
    - matches with a named group: pattern `(?<year>\d{4})` on "2026" exposes the group by name "year".
    - flag i: pattern `abc` with caseInsensitive matches "ABC".
    - flag m: anchored pattern matches per-line when multiline set; flag s: `.` matches newline when dotMatchesLineSeparators set; flag x: whitespace/comments ignored.
    - substitute: pattern `(\w+)` template `$1!` on "hi" yields "hi!".
    - garbage pattern `(` returns .failure (NO crash); empty input returns success with zero matches (no crash).
  </behavior>
  <action>Create `enum RegexTransformer` (`import Foundation` ONLY — no UI). Define a nested `enum TransformError: LocalizedError` and a `struct RegexMatch` carrying: full-match `NSRange` and matched substring, per-group results (numbered via `match.range(at:)`, named via `match.range(withName:)` using `numberOfCaptureGroups`), and the match index/position (character offset). Implement `static func matches(pattern: String, flags: Set<RegexFlag>, in text: String) -> Result<[RegexMatch], TransformError>` and `static func substitute(pattern: String, flags: Set<RegexFlag>, in text: String, template: String) -> Result<String, TransformError>`. Define `enum RegexFlag { case g, i, m, s, x }`. Map i→`.caseInsensitive`, m→`.anchorsMatchLines`, s→`.dotMatchesLineSeparators`, x→`.allowCommentsAndWhitespace`; `g` is NOT an NSRegularExpression.Options value — it selects enumerate-all (`matches(in:)`) vs first-only (`firstMatch(in:)`). Build the NSRegularExpression in a `do/catch`; an invalid pattern returns `.failure` with `error.localizedDescription` (never force-unwrap, INFRA-17). The transformer is PURE and SYNCHRONOUS — timeout/threading live in the ViewModel (Task 2), NOT here, so this stays unit-testable. Add an input size guard like JSONTransformer's 50MB guard. Write `FlintTests/RegexTransformerTests.swift` (XCTest, `@testable import Flint`) covering every behavior above plus the garbage/empty no-crash cases.</action>
  <verify>
    <automated>xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -only-testing:FlintTests/RegexTransformerTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Tools/Regex/RegexTransformer.swift` declares `enum RegexTransformer` and imports only Foundation (grep: no `import SwiftUI`/`import AppKit`).
    - `matches(pattern:flags:in:)` returns `Result<[RegexMatch], ...>`; `substitute(...)` returns `Result<String, ...>`.
    - RegexMatch exposes numbered AND named capture groups and the match position/index.
    - `xcodebuild test ... -only-testing:FlintTests/RegexTransformerTests` passes (exit 0), including the invalid-pattern `(` and empty-input cases.
  </acceptance_criteria>
  <done>Pure regex engine with full/named/numbered groups, flag mapping, replace, and no-crash guarantees, fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: RegexViewModel — never-freeze off-main eval (300ms debounce + 2s timeout)</name>
  <files>Tools/Regex/RegexViewModel.swift</files>
  <read_first>
    - Tools/JSONFormatter/JSONFormatterViewModel.swift (debounce + last-good + outputDimmed + injected onSaveHistory + ToolShortcutActions conformance; the project-wide `actor Debounce`)
    - Tools/Hash/HashViewModel.swift (Task store + cancel-in-flight + @MainActor.run publish pattern, lines 105-142)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §3 (the withThrowingTaskGroup timeout race; Task.isCancelled inside enumerateMatches; off-MainActor eval)
    - .planning/phases/02-extended-tools/02-UI-SPEC.md "Tool 1: Regex Tester" Interaction block (D-02 exact behavior + copy strings)
  </read_first>
  <action>Create `@Observable @MainActor final class RegexViewModel: ToolShortcutActions`. Observable state: `pattern`, `testString`, `flags: Set<RegexFlag>`, `template`, `replaceMode: Bool`, computed match results `[RegexMatch]`, `matchCountText`, `substitutionPreview`, `outputDimmed: Bool`, `errorMessage: String?`, and a `timedOut: Bool` (drives the WarningBannerView). Inject `onSaveHistory: (HistoryEntry) -> Void`; never import GRDB. On any change to pattern/testString/flags/template, call `scheduleEval()`: reuse the project-wide `Debounce` actor at 300ms, then launch evaluation OFF the MainActor via a `Task`, racing `RegexTransformer.matches(...)` against `Task.sleep(for: .seconds(2))` in a `withThrowingTaskGroup` — whichever finishes first wins; `cancelAll()` the loser. Cancel the in-flight eval Task on every keystroke (copy the cancel/store shape from HashViewModel). On timeout: set `timedOut = true`, errorMessage = "Pattern too slow — possible catastrophic backtracking", and keep the last successful matches with `outputDimmed = true` (CF-02). On invalid pattern: set errorMessage = "Invalid pattern: \(localizedDescription)" and dim. On success: publish matches on `@MainActor`, clear timeout/error, build matchCountText ("N match"/"N matches"/"No matches"), and call onSaveHistory with `HistoryEntry(tool: "regex", input: pattern + "\n" + testString, output: matchCountText, timestamp: Date(), pinned: false)`. Implement `primaryOutput()` (match-count text plus substitution preview when replaceMode) and `clearInput()` (clears pattern and testString). NOTE: because the transformer is synchronous, evaluating it inside an off-main Task is what keeps the UI thread free; also check `Task.isCancelled` inside the NSRegularExpression enumeration (transformer already structured for this — pass a cancellation check or chunk the call so the 2s wall-clock is the real safety net).</action>
  <verify>
    <automated>grep -q "withThrowingTaskGroup" Tools/Regex/RegexViewModel.swift && grep -q "ToolShortcutActions" Tools/Regex/RegexViewModel.swift && ! grep -q "import GRDB" Tools/Regex/RegexViewModel.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Class is `@Observable @MainActor final class RegexViewModel: ToolShortcutActions`.
    - File contains `withThrowingTaskGroup` (the 2s timeout race) and a debounce at 300ms via the existing Debounce actor.
    - In-flight eval Task is stored and cancelled on each new input (cancel-in-flight).
    - On timeout: `timedOut`/`errorMessage` set AND prior matches retained with `outputDimmed = true` (never blanks).
    - No `import GRDB`; history written only via injected `onSaveHistory`.
    - `primaryOutput()` and `clearInput()` implemented.
    - Project builds (exit 0).
  </acceptance_criteria>
  <done>Regex evaluation runs off-main with a 300ms debounce, 2s timeout, cancel-in-flight, and keep-last-good-dimmed — the UI cannot freeze.</done>
</task>

<task type="auto">
  <name>Task 3: RegexView (vertical stack + color-coded highlights) + RegexDefinition</name>
  <files>Tools/Regex/RegexView.swift, Tools/Regex/RegexDefinition.swift, Flint.xcodeproj/project.pbxproj</files>
  <read_first>
    - Tools/JSONFormatter/JSONFormatterView.swift (Convention A lazy onAppear view + @Bindable content view + .toolShortcuts)
    - Tools/Hash/HashView.swift + Tools/Hash/HashDefinition.swift (multi-row layout; HashViewWrapper history-injection convention B if preferred)
    - Tools/Base64/Base64Definition.swift (Definition WITH a detectionPredicate — analog for the conservative /…/ predicate)
    - UI/Components/SyntaxEditorView.swift (re-render guard; attribute-only highlight passes — Pitfall #5)
    - .planning/phases/02-extended-tools/02-UI-SPEC.md "Tool 1: Regex Tester" (exact layout diagram, capture-group palette, all copy strings, accessibility labels)
    - .planning/phases/02-extended-tools/02-PATTERNS.md "All five *View.swift" + "All five *Definition.swift"
  </read_first>
  <action>Create `RegexView` following Convention A (lazy `@State var viewModel` built in `.onAppear` with `onSaveHistory` from the `HistoryStore` environment) OR Convention B wrapper — match an existing analog. Build the vertical stack from the UI-SPEC diagram: HStack of pattern `TextField` (monospaced 13pt, placeholder "Regular expression", accessibilityLabel "Regex pattern") + a "Patterns ▾" `Menu` inserting Email / URL (HTTP/HTTPS) / Phone (US) / Date (ISO 8601) / IPv4 Address preset patterns (static dictionary, RGX-04); a row of five `Toggle(.checkbox)` flag controls labeled "g (global)".."x (verbose)" each with `.accessibilityLabel`; `Divider()`; the test-string `SyntaxEditorView(text:accessibilityLabel:"Test string")` with a trailing match-count badge caption; `Divider()`; a collapsible `DisclosureGroup("Match Results")` rendering index / position / full match / capture-group columns (SwiftUI `Table` preferred for window, `List` acceptable); `Divider()`; a collapsible `DisclosureGroup("Replace")` with a template `TextField` and a read-only `CodeDisplayView` substitution preview carrying a `CopyButtonView`. Show `WarningBannerView(message: viewModel.errorMessage ?? "", severity: .warning)` when `timedOut`; show `InlineErrorView(message:)` for invalid-pattern. Apply per-capture-group highlight backgrounds in the SyntaxEditorView via an attribute-only `NSTextStorage` pass using the UI-SPEC palette (group 0 accent .25, group 1 yellow .30, etc., cycling) — MUST be re-entrancy guarded and MUST NOT reset `.string` (Pitfall #5). Dim the highlight at `.opacity(0.4)` when `outputDimmed`. Apply `.toolShortcuts(viewModel)`. Create `enum RegexDefinition { static func make() -> ToolDefinition }` with id "regex", name "Regex Tester", category `.analysis`, keywords ["regex","regexp","pattern","match","replace","grep"], sfSymbol "text.magnifyingglass", and `makeView: { @MainActor in AnyView(RegexView(...)) }`. For `detectionPredicate`: per UI-SPEC, EITHER a conservative `/pattern/flags` slash-literal matcher OR `nil` (search-only) — choose and document the choice in a code comment (RESEARCH §0 warns an aggressive regex predicate hijacks the whole chain; nil is the safe default). Do NOT register in ToolRegistry here — that is the Wave-7 plan. Add the two new files to the Flint target in project.pbxproj.</action>
  <verify>
    <automated>grep -q "RegexDefinition" Tools/Regex/RegexDefinition.swift && grep -q "category: .analysis" Tools/Regex/RegexDefinition.swift && grep -q "toolShortcuts" Tools/Regex/RegexView.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `RegexDefinition.make()` returns a ToolDefinition with id "regex" and category `.analysis`.
    - The detectionPredicate decision (conservative `/…/` or nil) is implemented AND documented in a code comment per RESEARCH §0.
    - `RegexView` renders pattern field + Patterns menu + five flag toggles + test editor + collapsible results table + collapsible replace section (matches UI-SPEC layout).
    - Capture-group highlighting is applied as attribute-only NSTextStorage background colors and is re-entrancy guarded (no `.string` assignment in the highlight pass).
    - Every TextField/Toggle/Button/Menu has an `.accessibilityLabel` (INFRA-15).
    - `.toolShortcuts(viewModel)` is applied; copy strings match the UI-SPEC Copywriting table.
    - Project builds (exit 0). RegexDefinition.make() is NOT yet appended to ToolRegistry.tools.
  </acceptance_criteria>
  <done>A fully laid-out, accessible Regex tool view + definition, built against the never-freeze ViewModel, ready for Wave-7 registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → regex engine | Untrusted pattern can trigger catastrophic backtracking (ReDoS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-RGX-DoS | Denial of Service | RegexViewModel eval | mitigate | Off-MainActor eval + 300ms debounce + 2s withThrowingTaskGroup timeout + cancel-in-flight (D-02); UI thread never blocked |
| T-02-RGX-IV | Input Validation | RegexTransformer | mitigate | Invalid pattern → Result.failure (no force-unwrap); 50MB input guard; empty/garbage tested (INFRA-17) |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:FlintTests/RegexTransformerTests` passes.
- `xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS'` succeeds.
- MANUAL/UAT (RESEARCH §10, cannot be unit-tested): paste pattern `(a+)+$` against `"aaaaaaaaaaaaaaaaaaaa!"` — UI stays responsive, the timeout warning shows, the last-good highlight stays dimmed.
</verification>

<success_criteria>
RGX-01..04 met: pattern+flags+multiline test string; live color-coded per-group highlighting with a count badge; results table with named+numbered groups; replace preview + pattern library. The UI never freezes on pathological input. Tool is built and ready to register.
</success_criteria>

<output>
Create `.planning/phases/02-extended-tools/02-02-SUMMARY.md` when done.
</output>
