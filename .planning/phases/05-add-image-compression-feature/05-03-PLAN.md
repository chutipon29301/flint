---
phase: 05-add-image-compression-feature
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - Tools/ImageCompress/ImageCompressView.swift
  - Tools/ImageCompress/ImageCompressDefinition.swift
  - Core/Services/ToolRegistry.swift
  - Flint.xcodeproj/project.pbxproj
autonomous: true
requirements: [D-01, D-04, D-05, D-09, D-10, INFRA-14, INFRA-15, INFRA-17]
must_haves:
  truths:
    - "The Image Compressor appears in the launcher tool list / fuzzy search and opens its view"
    - "D-01: Dropping one OR many image files starts a batch and the results table fills in live (one row per image)"
    - "D-04: The quality slider (0-100) + Web/Email/Max presets are present; D-05: the slider is disabled when the whole batch is lossless (PNG/TIFF)"
    - "D-09: Each row shows thumbnail, filename, format tag, original to new size, and % saved (green when saved, secondary when grew)"
    - "INFRA-17: A non-image / corrupt dropped file shows a per-row warning, never crashes the app; D-10: no side-by-side before/after comparison pane"
  artifacts:
    - path: "Tools/ImageCompress/ImageCompressView.swift"
      provides: "Drop surface + DropOverlayView + quality slider/presets + results table"
      contains: "DropOverlayView"
      min_lines: 90
    - path: "Tools/ImageCompress/ImageCompressDefinition.swift"
      provides: "ToolDefinition.make() + HistoryStore env wrapper"
      contains: "detectionPredicate: nil"
      min_lines: 25
    - path: "Core/Services/ToolRegistry.swift"
      provides: "Registration of ImageCompressDefinition.make()"
      contains: "ImageCompressDefinition.make()"
  key_links:
    - from: "ImageCompressView"
      to: "viewModel.compress(urls:quality:)"
      via: "multi-provider .onDrop joins all providers then calls compress once"
      pattern: "viewModel.compress"
    - from: "ImageCompressDefinition"
      to: "ImageCompressView(onSaveHistory:)"
      via: "ImageCompressViewWrapper injects @Environment(HistoryStore.self)"
      pattern: "HistoryStore.self"
    - from: "ToolRegistry.tools"
      to: "ImageCompressDefinition.make()"
      via: "sanctioned append to the tools array"
      pattern: "ImageCompressDefinition.make"
---

<objective>
Build the Image Compressor UI and wire it into the app: `ImageCompressView` (multi-file drop surface + `DropOverlayView` + quality slider/presets + the net-new results table with live per-row progress), `ImageCompressDefinition` (the `ToolDefinition.make()` + HistoryStore environment wrapper, `detectionPredicate: nil`), and a single sanctioned-append registration line in `ToolRegistry`. After this plan the tool is reachable from the launcher and works end-to-end.

Purpose: Surface the batch ViewModel through SwiftUI exactly per the APPROVED UI-SPEC, reusing the Hash drop idiom + `DropOverlayView` + `WarningBannerView`, and registering the tool the same sanctioned way the five Phase-2 tools were appended. The results table, multi-file drop, and quality controls are net-new (no in-codebase analog) and come from UI-SPEC + RESEARCH.

Output: `ImageCompressView.swift`, `ImageCompressDefinition.swift`, the `ToolRegistry.swift` registration edit, all added/updated in the Flint target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-add-image-compression-feature/05-CONTEXT.md
@.planning/phases/05-add-image-compression-feature/05-RESEARCH.md
@.planning/phases/05-add-image-compression-feature/05-PATTERNS.md
@.planning/phases/05-add-image-compression-feature/05-UI-SPEC.md

<interfaces>
<!-- Contracts this plan CONSUMES (from plan 05-02): -->
@Observable @MainActor final class ImageCompressViewModel: ToolShortcutActions
  var rows: [CompressRow]; var isCompressing: Bool
  init(onSaveHistory: @escaping (HistoryEntry) -> Void)
  func compress(urls: [URL], quality: Double)   // quality is 0.0-1.0
  func cancel(); func primaryOutput() -> String?; func clearInput()
CompressRow { id, sourceURL, format: ImageFormatTag, state: .pending/.compressing/.done(CompressedImage)/.failed(reason) }
ImageFormatTag { displayTag: String, isLossless: Bool, static from(url:) }

<!-- FROZEN contract this plan conforms to: -->
@Core/Models/ToolDefinition.swift
  ToolDefinition(id, name, category: ToolCategory, keywords, sfSymbol, detectionPredicate, makeView)
  // ToolCategory cases: .encoding .formatting .conversion .generation .analysis (use .conversion or .analysis)
</interfaces>

Analogs:
@Tools/Hash/HashDefinition.swift   (entire file — Definition + private *ViewWrapper env injection; copy + rename)
@Tools/Hash/HashView.swift         (lines 5-28 view-owned VM + init; lines 45-61 .onDrop + DropOverlayView overlay; line 42 .toolShortcuts; lines 257-268 NSOpenPanel)
@Core/Services/ToolRegistry.swift  (lines 15-36 tools array + sanctioned-append precedent + freeze note)

Reused components:
@UI/Components/DropOverlayView.swift    (init: DropOverlayView(label: String))
@UI/Components/WarningBannerView.swift  (init: WarningBannerView(message: String, severity: .warning/.error))
@UI/Components/ToolShortcutActions.swift (line 96 .toolShortcuts(_:) modifier)

Net-new surfaces — source of truth (no in-codebase analog):
@.planning/phases/05-add-image-compression-feature/05-UI-SPEC.md   (§ Drop Surface, § Quality Slider + Presets, § Results Table, § Empty State, Copywriting Contract, Accessibility Contract)
@.planning/phases/05-add-image-compression-feature/05-RESEARCH.md   (Code Examples lines 391-413 multi-provider drop snippet; lines 369-389 slider-to-quality mapping + lossy/lossless detection)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImageCompressView — multi-file drop + quality slider/presets + results table</name>
  <files>Tools/ImageCompress/ImageCompressView.swift</files>
  <read_first>
    - 05-UI-SPEC.md § Interaction Contracts (lines 132-189) — Drop Surface (D-01 multi-provider), Quality Slider + Presets (D-04/D-05 with the Web=60/Email=75-default/Max=95 table), Results Table (D-09 column order, per-row states, cancellation), Empty State, No Side-by-Side (D-10).
    - 05-UI-SPEC.md Copywriting Contract (lines 196-223) — every literal string (tool title, drop label, "Quality", preset names, lossless helper line, results heading, empty state, format tags, size delta, % saved formats, failure messages, a11y strings) — use these EXACTLY.
    - 05-UI-SPEC.md Spacing/Typography/Color (lines 47-129) — 40pt thumbnail cornerRadius(4), 56pt row height, Divider separators, results container .padding(10) + .background(.quaternary.opacity(0.3)) + .cornerRadius(8), monospaced size digits, green vs secondary % saved, accent only where listed.
    - 05-UI-SPEC.md Accessibility Contract (lines 261-272) — slider/preset/row/thumbnail/progress/cancel a11y labels (INFRA-15).
    - Tools/Hash/HashView.swift (lines 5-28, 42, 45-61, 257-268) — view-owned VM + init, .toolShortcuts, .onDrop + DropOverlayView overlay, NSOpenPanel idiom.
    - 05-RESEARCH.md Code Examples (lines 391-413) — verified multi-provider DispatchGroup drop snippet (Hash reads providers.first only; this tool iterates ALL providers); (lines 369-389) slider/100 mapping + isLossy detection.
    - user MEMORY.md pitfall — if quality persists, bind to @AppStorage("imageCompressQuality") directly, NOT a computed PreferencesStore property.
  </read_first>
  <action>
    Create `struct ImageCompressView: View` (imports `SwiftUI`, `AppKit`, `UniformTypeIdentifiers`) owning `@State private var viewModel: ImageCompressViewModel`, `@State private var isDragTargeted = false`, and the quality value as `@AppStorage("imageCompressQuality") private var quality: Double = 75` (per MEMORY pitfall — bind the Slider directly to this, never to a computed PreferencesStore prop). `init(onSaveHistory:)` constructs the VM via `_viewModel = State(initialValue: ImageCompressViewModel(onSaveHistory: onSaveHistory))` — mirror HashView lines 26-28.

    Body = a `ScrollView { VStack(alignment: .leading, spacing: 16) { … } .padding() }` with the tool title "Image Compressor" (15pt semibold, .primary), then the quality controls section, then the results section. Attach `.toolShortcuts(viewModel)`.

    Drop surface (D-01, net-new multi-file): `.onDrop(of: [.fileURL], isTargeted: $isDragTargeted)` on the root, iterating ALL providers via a `DispatchGroup` (NOT `providers.first`) per RESEARCH lines 391-413: for each provider `loadItem(forTypeIdentifier: UTType.fileURL.identifier)`, collect URLs, then `group.notify(queue: .main) { Task { @MainActor in viewModel.compress(urls: urls, quality: quality / 100.0) } }` — note the slider/100 map to ImageIO 0.0-1.0 (RESEARCH lines 369-378). `.overlay { if isDragTargeted { DropOverlayView(label: "Drop images to compress").transition(.opacity.animation(.easeOut(duration: 0.15))) } }`.

    Quality controls (D-04/D-05): a labeled section with `Slider(value: $quality, in: 0...100, step: 1)` (label "Quality" left, "{Int(quality)}%" right) plus three preset `Button`s in an `HStack(spacing: 8)`: Web=60, Email=75, Max=95. Clicking a preset sets `quality`; the preset whose value == quality renders `.borderedProminent`, others `.bordered`. Lossless gating: compute whether the current batch (`viewModel.rows`) is ENTIRELY lossless via `row.format.isLossless`; if so, `.disabled(true)` + `.secondary` tint on the slider and show the helper line "PNG and TIFF are lossless — they're re-encoded, but quality doesn't apply." In a mixed/empty batch the slider stays enabled. Add the slider/preset accessibility labels from the UI-SPEC (INFRA-15). Use only SwiftUI semantic colors — no hardcoded hex (INFRA-14).

    Results table (D-09, net-new): an empty-state branch showing centered 13pt .secondary "Drop images here to compress them." when `viewModel.rows.isEmpty`; otherwise a "Results" heading + a `VStack` of rows separated by `Divider()`, the whole block in the .padding(10) + .background(.quaternary.opacity(0.3)) + .cornerRadius(8) container. Each row (min height 56pt) is an `HStack`: (1) 40x40pt thumbnail via `Image(nsImage: NSImage(contentsOf: row.sourceURL) ?? placeholder)` clipped to `RoundedRectangle(cornerRadius: 4)` (load lazily; a .quaternary placeholder while nil — thumbnail rendering lives HERE in the View, never the Transformer); (2) a `VStack(alignment:.leading, spacing:2)` of filename (13pt, .lineLimit(1), .truncationMode(.middle)) over the 10pt .secondary `row.format.displayTag`; (3)+(4) state-driven trailing content: `.pending` shows dim "—"; `.compressing` shows `ProgressView().scaleEffect(0.6)`; `.done(img)` shows "{orig} → {new}" via `ByteCountFormatter` (.file style, 13pt monospaced .secondary) and the % saved as Caption 11pt semibold — Color.green "−{n}%" when `img.percentSaved > 0`, Color.secondary "+{n}%"/"0%" otherwise (honest grew-larger reporting, D-05); `.failed(reason)` replaces the trailing columns with `WarningBannerView(message: reason, severity: .warning)`. Mark each row `.accessibilityElement(children: .combine)` with the UI-SPEC row label. While `viewModel.isCompressing`, show a "Cancel" `Button` (.foregroundStyle(.red)) calling `viewModel.cancel()`.

    Do NOT add any before/after side-by-side comparison pane (D-10). Optionally include a "Choose Images…" `Button` using `NSOpenPanel` with `allowsMultipleSelection = true`, `canChooseFiles = true`, `allowedContentTypes = [.png, .jpeg, .heic, .tiff]` calling `viewModel.compress(urls:quality:)` (optional this phase — drag-drop is primary).
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && grep -q 'DropOverlayView(label: "Drop images to compress")' Tools/ImageCompress/ImageCompressView.swift && grep -q "viewModel.compress(urls:" Tools/ImageCompress/ImageCompressView.swift && grep -q "imageCompressQuality" Tools/ImageCompress/ImageCompressView.swift && grep -q "WarningBannerView" Tools/ImageCompress/ImageCompressView.swift && grep -q "quality / 100" Tools/ImageCompress/ImageCompressView.swift && ! grep -qi "providers.first" Tools/ImageCompress/ImageCompressView.swift && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - View owns `@State viewModel: ImageCompressViewModel`, has `init(onSaveHistory:)`, and attaches `.toolShortcuts(viewModel)` (mirrors HashView).
    - `.onDrop` iterates ALL providers (a `DispatchGroup`/multi-provider join) and calls `viewModel.compress(urls:quality:)` ONCE — the string `providers.first` does NOT appear (D-01 batch).
    - Slider value maps to ImageIO quality via `quality / 100.0` at the call site (RESEARCH lines 369-378); slider bound to `@AppStorage("imageCompressQuality")` (MEMORY pitfall), default 75.
    - Three presets Web(60)/Email(75)/Max(95) present; active preset uses `.borderedProminent` (D-04).
    - When every row is lossless (PNG/TIFF) the slider is `.disabled(true)` and the lossless helper line shows the exact UI-SPEC copy (D-05); mixed/empty keeps it enabled.
    - Results rows render thumbnail (View-side `NSImage`, not Transformer), filename + `format.displayTag`, "{orig} → {new}" (ByteCountFormatter), and % saved green-when-saved / secondary-when-grew (D-09).
    - `.failed` rows render `WarningBannerView(severity: .warning)` — no crash path (INFRA-17).
    - No side-by-side/comparison/preview pane (D-10).
    - All literal copy strings match the UI-SPEC Copywriting Contract exactly; only SwiftUI semantic colors used (INFRA-14); a11y labels present on slider/presets/rows/cancel (INFRA-15).
  </acceptance_criteria>
  <done>`ImageCompressView` renders the approved drop + slider/presets + live results table, maps slider to ImageIO quality, gates lossless, and surfaces failures as warnings — verified by the compile in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: ImageCompressDefinition + ToolRegistry registration + Xcode target membership + end-to-end build</name>
  <files>Tools/ImageCompress/ImageCompressDefinition.swift, Core/Services/ToolRegistry.swift, Flint.xcodeproj/project.pbxproj</files>
  <read_first>
    - Tools/Hash/HashDefinition.swift (entire file, lines 1-34) — copy wholesale and rename: enum *Definition { static func make() -> ToolDefinition } + private *ViewWrapper reading @Environment(HistoryStore.self) and passing historyStore.save into the View's init(onSaveHistory:).
    - 05-PATTERNS.md "ImageCompressDefinition" (lines 19-57) — concrete substitutions: id "image-compress", name "Image Compressor", category (.conversion or .analysis), keywords list, sfSymbol "photo", detectionPredicate: nil MANDATORY.
    - Core/Services/ToolRegistry.swift (lines 1-37) — the FROZEN file with the SANCTIONED-APPEND precedent (lines 23-36). Confirm no NEWER freeze directive supersedes the sanctioned append (05-PATTERNS.md lines 204-218, CONTEXT integration-points).
    - Core/Models/ToolDefinition.swift (lines 8-19) — the frozen struct shape to conform to; detectionPredicate is (@Sendable (String) -> DetectionResult?)? (TEXT-only → nil for an image tool).
    - Core/Models/ToolCategory.swift — confirm the case to use (.conversion or .analysis).
    - 05-RESEARCH.md A5 (line 434) — the new Definition + View files need explicit Flint-target membership in project.pbxproj.
  </read_first>
  <action>
    Create `Tools/ImageCompress/ImageCompressDefinition.swift` by mirroring `HashDefinition.swift`: `enum ImageCompressDefinition { static func make() -> ToolDefinition }` returning `ToolDefinition(id: "image-compress", name: "Image Compressor", category: <.conversion or .analysis — pick by reading ToolCategory>, keywords: ["image","compress","jpeg","png","heic","tiff","optimize","shrink","photo"], sfSymbol: "photo", detectionPredicate: nil, makeView: { @MainActor in AnyView(ImageCompressViewWrapper()) })`. `detectionPredicate: nil` is MANDATORY — the image tool has no text to detect (like Hash); this keeps `ToolRegistry.detect()` skipping it via the existing optional chain. Add the private `struct ImageCompressViewWrapper: View { @Environment(HistoryStore.self) private var historyStore; var body: some View { ImageCompressView { entry in historyStore.save(entry) } } }`.

    Edit `Core/Services/ToolRegistry.swift`: append `ImageCompressDefinition.make(),` to the `tools` array after `TextDiffDefinition.make()`, and extend the existing sanctioned-append comment to note this Phase-5 single-line addition (the same kind of mutation already present for the five Phase-2 tools). Make NO other change to the file — `search()` picks up the new keywords automatically and `detect()` skips it (nil predicate). If a NEWER freeze directive than the lines 23-36 sanctioned-append precedent is found at the top of the file, STOP and surface it instead of editing.

    Add the two new files (`Tools/ImageCompress/ImageCompressDefinition.swift`, `Tools/ImageCompress/ImageCompressView.swift`) to the **Flint** app target in `Flint.xcodeproj/project.pbxproj`, mirroring HashDefinition.swift / HashView.swift reference shapes. No SPM change (D-03). Then build the full app target to prove the entire 4-file tool + registration compiles and links into the app (the A5 target-membership gate).
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && grep -q "detectionPredicate: nil" Tools/ImageCompress/ImageCompressDefinition.swift && grep -q "HistoryStore.self" Tools/ImageCompress/ImageCompressDefinition.swift && grep -q "ImageCompressDefinition.make()" Core/Services/ToolRegistry.swift && xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ImageCompressDefinition.make()` returns a `ToolDefinition` with id "image-compress", name "Image Compressor", `detectionPredicate: nil` (MANDATORY — no clipboard detection, like Hash), sfSymbol "photo", and the image keywords.
    - Private `ImageCompressViewWrapper` injects `@Environment(HistoryStore.self)` and passes `historyStore.save` into `ImageCompressView(onSaveHistory:)` (mirrors HashDefinition).
    - `Core/Services/ToolRegistry.swift` contains exactly one new line `ImageCompressDefinition.make(),` appended to the `tools` array (sanctioned append); no other behavioral change; `search()`/`detect()` untouched.
    - All four ImageCompress files (Transformer, ViewModel, View, Definition) are members of the Flint app target in `project.pbxproj`; no new SPM package (D-03).
    - `xcodebuild build -scheme Flint` SUCCEEDS — the full 4-file tool + registration compiles and links into the app (A5 gate closed).
  </acceptance_criteria>
  <done>The tool is registered via a single sanctioned-append line, all four files are in the Flint target, and the full app builds — the Image Compressor is reachable from the launcher search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user drag-drop → View → ViewModel | Untrusted file set enters the app UI; must route to the batch pipeline without blocking or crashing |
| registry edit → frozen file | A frozen, behavior-critical file (ToolRegistry) is mutated; the edit must be the single sanctioned append, nothing more |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | drop of a non-image / corrupt file | mitigate | The View routes all dropped URLs to the ViewModel batch; bad files become `.failed` rows rendered as `WarningBannerView(.warning)` — no crash, no drag-time rejection style (INFRA-17, UI-SPEC § Drop Surface). |
| T-05-08 | Tampering | edit to the FROZEN ToolRegistry.swift | mitigate | Only a single sanctioned-append line is added (the established precedent for the five Phase-2 tools); `search()`/`detect()` are not touched. If a newer freeze directive is present, the executor STOPs rather than editing. |
| T-05-09 | Spoofing / misrouting | image tool participating in clipboard detection | mitigate | `detectionPredicate: nil` (MANDATORY) — the image tool never matches the text detection chain, so a crafted clipboard string cannot route to it (INFRA-06 chain integrity preserved). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — zero new external dependencies (D-03). |
</threat_model>

<verification>
- `xcodebuild build -scheme Flint` succeeds with all four ImageCompress files in the target.
- View: multi-provider drop (no `providers.first`), slider/100 quality map, lossless gate, WarningBannerView for failures, no comparison pane.
- Definition: `detectionPredicate: nil`, HistoryStore wrapper.
- Registry: single sanctioned-append line; `search()`/`detect()` unchanged.
</verification>

<success_criteria>
- The Image Compressor is registered and reachable from launcher search.
- Dropping one or many images runs the batch and fills the live results table (D-01, D-09).
- Quality slider + Web/Email/Max presets work; lossless batches disable the slider with the helper line (D-04/D-05).
- Rows show thumbnail + size delta + % saved (green/secondary); failures are warnings (D-09, INFRA-17).
- No side-by-side comparison (D-10); semantic colors only (INFRA-14); a11y labels present (INFRA-15); zero new deps (D-03).
</success_criteria>

<output>
Create `.planning/phases/05-add-image-compression-feature/05-03-SUMMARY.md` when done.
</output>
