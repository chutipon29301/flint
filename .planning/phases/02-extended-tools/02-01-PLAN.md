---
phase: 02-extended-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Flint.xcodeproj/project.pbxproj
  - Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - Tools/TextDiff/SwiftDiff/diff.swift
  - Tools/TextDiff/SwiftDiff/cleanup.swift
  - Tools/TextDiff/SwiftDiff/common.swift
  - Tools/TextDiff/SwiftDiff/String.swift
  - Tools/TextDiff/SwiftDiff/UnicodeScalar.swift
  - Tools/TextDiff/SwiftDiff/NSRegularExpression.swift
  - Resources/github-markdown.css
  - FlintTests/SwiftDiffVendorTests.swift
autonomous: false
requirements: [CLR-01, MD-01, MD-03, DIFF-02]
user_setup: []

must_haves:
  truths:
    - "The project builds with ChromaKit and swift-markdown resolved as SPM dependencies"
    - "The vendored SwiftDiff compiles under the Swift 6 toolchain and produces the documented diff output"
    - "A GitHub-flavored CSS stylesheet is bundled into the app and loadable from the main bundle"
  artifacts:
    - path: "Tools/TextDiff/SwiftDiff/diff.swift"
      provides: "Vendored, Swift-6-patched diff-match-patch core exposing func diff(text1:text2:timeout:) -> [Diff]"
      contains: "func diff"
    - path: "Resources/github-markdown.css"
      provides: "Self-contained GitHub-like Markdown stylesheet with prefers-color-scheme support"
      min_lines: 40
    - path: "FlintTests/SwiftDiffVendorTests.swift"
      provides: "Test proving vendored SwiftDiff matches its documented example output"
      contains: "SwiftDiff"
  key_links:
    - from: "Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved"
      to: "github.com/HarshilShah/ChromaKit"
      via: "SPM package pin 0.1.1"
      pattern: "ChromaKit"
    - from: "Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved"
      to: "github.com/swiftlang/swift-markdown"
      via: "SPM package pin"
      pattern: "swift-markdown"
---

<objective>
Land every new third-party dependency Phase 2 needs BEFORE any tool plan that imports them, in one dedicated foundation plan so the five tool plans never race on dependency resolution.

Purpose: ChromaKit (Color/OKLCH), swift-markdown (Markdown GFM AST), the vendored+patched SwiftDiff (Text Diff word-level), and the bundled GitHub CSS (Markdown export) are all prerequisites. SwiftDiff in particular CANNOT be added via SPM (its Package.swift is swift-tools-version:3.1 with no products/targets and its source uses Swift APIs removed in Swift 6) — it must be vendored and mechanically patched. Centralizing these here keeps the project file edits in one wave.
Output: Two resolved SPM packages, six vendored+patched Swift files under Tools/TextDiff/SwiftDiff/, a bundled CSS resource, and a vendor test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extended-tools/02-RESEARCH.md
@.planning/phases/02-extended-tools/02-PATTERNS.md
@CLAUDE.md

# Authoritative facts (from RESEARCH.md §2, VERIFIED from package source):
# - ChromaKit: https://github.com/HarshilShah/ChromaKit  — pin EXACT 0.1.1 (swift-tools-version 5.9, macOS 10.15+; forward-only OKLCH→color)
# - swift-markdown (product name "Markdown"): https://github.com/swiftlang/swift-markdown — up-to-next-major from 0.8.0 (Apple-owned, pulls swift-cmark gfm transitively)
# - SwiftDiff: github.com/turbolent/SwiftDiff — VENDOR, do NOT add via SPM. Last commit 2017, Package.swift swift-tools-version:3.1 with no products/targets, source uses .characters / substring(to:/from:) (removed in Swift 6).
# - Current Package.resolved already has: GRDB 7.11.1, HighlightSwift 1.1.0, KeyboardShortcuts 3.0.1, MenuBarExtraAccess 1.3.0, UUIDv7
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before adding (ChromaKit + swift-markdown)</name>
  <what-built>Nothing yet — this is a pre-install legitimacy gate. RESEARCH.md §2 verified both packages from source (ChromaKit tag 0.1.1 published 2025-01-31; swift-markdown release 0.8.0 published 2026-05-07, owner swiftlang/Apple). swift-markdown is Apple-owned and low-risk. ChromaKit is a single-maintainer package (45 stars, exact pin mitigates supply-chain drift).</what-built>
  <how-to-verify>
    1. Open https://github.com/swiftlang/swift-markdown — confirm owner is `swiftlang` (Apple) and a 0.8.0 release exists.
    2. Open https://github.com/HarshilShah/ChromaKit — confirm tag `0.1.1` exists and Package.swift declares `swift-tools-version: 5.9`.
    3. Confirm you are comfortable pinning ChromaKit to EXACT 0.1.1 (no auto-upgrade).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding the packages, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add ChromaKit + swift-markdown via SPM</name>
  <files>Flint.xcodeproj/project.pbxproj, Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved</files>
  <read_first>
    - Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved (current pinned packages — append, do not reshape)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §2 (package URLs, pins, provenance — VERIFIED)
    - CLAUDE.md "What NOT to Use" table (do not substitute Ink/Highlightr)
  </read_first>
  <action>Add two Swift Package dependencies to the Flint target. ChromaKit from `https://github.com/HarshilShah/ChromaKit` pinned EXACT version `0.1.1` (use `.exact("0.1.1")` semantics — exact pin, no auto-upgrade); link its `ChromaKit` product to the Flint target. swift-markdown from `https://github.com/swiftlang/swift-markdown` using up-to-next-major from `0.8.0`; link its product named `Markdown` to the Flint target. Do this via Xcode "Add Package" (preferred — updates pbxproj + Package.resolved consistently) or by editing the XCRemoteSwiftPackageReference / XCSwiftPackageProductDependency entries directly. Do NOT add SwiftDiff via SPM (Task 4 vendors it). Do NOT remove or re-pin any existing package (GRDB, HighlightSwift, KeyboardShortcuts, MenuBarExtraAccess, UUIDv7).</action>
  <verify>
    <automated>grep -q "ChromaKit" Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved && grep -q "swift-markdown" Flint.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Package.resolved contains a pin entry for `ChromaKit` at identity/version `0.1.1`.
    - Package.resolved contains a pin entry for `swift-markdown`.
    - All five pre-existing packages (GRDB, HighlightSwift, KeyboardShortcuts, MenuBarExtraAccess, UUIDv7) remain present and unchanged in Package.resolved.
    - `xcodebuild -resolvePackageDependencies -project Flint.xcodeproj -scheme Flint` succeeds (exit 0).
  </acceptance_criteria>
  <done>Both packages resolve and link to the Flint target; existing pins untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor + Swift-6-patch SwiftDiff and bundle the Markdown CSS</name>
  <files>Tools/TextDiff/SwiftDiff/diff.swift, Tools/TextDiff/SwiftDiff/cleanup.swift, Tools/TextDiff/SwiftDiff/common.swift, Tools/TextDiff/SwiftDiff/String.swift, Tools/TextDiff/SwiftDiff/UnicodeScalar.swift, Tools/TextDiff/SwiftDiff/NSRegularExpression.swift, Resources/github-markdown.css, Flint.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/02-extended-tools/02-RESEARCH.md §2 (vendor + patch instructions: ~72 mechanical edits, the exact `.characters`→`` substitution list, the public surface to preserve)
    - .planning/phases/02-extended-tools/02-PATTERNS.md "No Analog Found" section (vendored SwiftDiff handling)
    - https://github.com/turbolent/SwiftDiff (source of the 6 files to copy: diff.swift, cleanup.swift, common.swift, String.swift, UnicodeScalar.swift, NSRegularExpression.swift, ~38 KB total)
  </read_first>
  <action>Copy the six SwiftDiff source files from github.com/turbolent/SwiftDiff into `Tools/TextDiff/SwiftDiff/` and apply the mechanical Swift-6 patch (~72 sites): replace `.characters.count`→`.count`, `.characters.first`→`.first`, `.characters.last`→`.last`, `characters.distance(...)`→`distance(...)`; replace the deprecated `substring(to:)`/`substring(from:)` wrappers with the package's already-valid `subscript(range: Range<Int>)`. PRESERVE the public surface exactly: `public func diff(text1: String, text2: String, timeout: CFTimeInterval? = nil) -> [Diff]` and `public enum Diff: Equatable { case equal(String); case insert(String); case delete(String); var text: String }`. Use only the diff algorithm — do not vendor match/patch. Separately, create `Resources/github-markdown.css`: a self-contained GitHub-flavored stylesheet (typography, tables, blockquotes, fenced-code blocks, task-list checkboxes, strikethrough) with a `@media (prefers-color-scheme: dark)` block so the preview adapts to macOS Light/Dark (D-11). Add all seven new files to the Flint target's Sources/Resources build phases in project.pbxproj (CSS as a bundle resource). Exact CSS contents are Claude's Discretion per CONTEXT.md.</action>
  <verify>
    <automated>test -f Tools/TextDiff/SwiftDiff/diff.swift && test -f Resources/github-markdown.css && grep -q "prefers-color-scheme" Resources/github-markdown.css && ! grep -rn "\.characters" Tools/TextDiff/SwiftDiff/ && grep -q "func diff" Tools/TextDiff/SwiftDiff/diff.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist under `Tools/TextDiff/SwiftDiff/`.
    - No file under `Tools/TextDiff/SwiftDiff/` contains the token `.characters` (grep returns no matches).
    - `Tools/TextDiff/SwiftDiff/diff.swift` declares `public func diff(text1:` and the source contains `public enum Diff` with cases `equal`, `insert`, `delete`.
    - `Resources/github-markdown.css` exists, is ≥ 40 lines, and contains a `@media (prefers-color-scheme: dark)` rule.
    - The CSS file is referenced as a resource in project.pbxproj (so `Bundle.main.url(forResource:"github-markdown", withExtension:"css")` resolves at runtime).
  </acceptance_criteria>
  <done>SwiftDiff vendored and Swift-6-clean; CSS bundled; all seven files in the build target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Vendor test proving SwiftDiff produces the documented output</name>
  <files>FlintTests/SwiftDiffVendorTests.swift</files>
  <read_first>
    - Tools/TextDiff/SwiftDiff/diff.swift (the vendored public API)
    - FlintTests/JSONTransformerTests.swift (XCTest analog shape)
    - .planning/phases/02-extended-tools/02-RESEARCH.md §2 (documented example output)
  </read_first>
  <behavior>
    - Test 1: diff(text1: "Hello World", text2: "Goodbye World") returns a [Diff] whose concatenated `.text` values reconstruct "Goodbye World" when keeping `.equal` + `.insert` and reconstruct "Hello World" when keeping `.equal` + `.delete`.
    - Test 2: diff of two identical strings returns only `.equal` segments (no `.insert`/`.delete`).
    - Test 3: diff of "" vs "abc" returns a single `.insert("abc")`-equivalent (only insert/equal, no delete).
  </behavior>
  <action>Create `FlintTests/SwiftDiffVendorTests.swift` (XCTest, `@testable import Flint`) asserting the three behaviors above against the vendored `diff(text1:text2:)`. Reconstruct each side by folding the `[Diff]` and assert equality with the inputs. This is the supply-chain guard for the vendored code (RESEARCH §11 threat: vendored diff tampering). Add the test file to the FlintTests target.</action>
  <verify>
    <automated>xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -only-testing:FlintTests/SwiftDiffVendorTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `FlintTests/SwiftDiffVendorTests.swift` exists and is in the FlintTests target.
    - The three behaviors above are asserted as test methods.
    - `xcodebuild test ... -only-testing:FlintTests/SwiftDiffVendorTests` passes (exit 0).
  </acceptance_criteria>
  <done>Vendored SwiftDiff is proven correct and Swift-6-compilable by a passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party package source → Flint binary | ChromaKit/swift-markdown pulled from GitHub; vendored SwiftDiff copied into repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | SPM package installs (ChromaKit, swift-markdown) | mitigate | Pre-install legitimacy checkpoint (Task 1, blocking-human); ChromaKit pinned EXACT 0.1.1; swift-markdown Apple-owned |
| T-02-VD | Tampering | Vendored SwiftDiff source | mitigate | Full source inspected before vendoring (RESEARCH §2); no network/no dynamic exec; SwiftDiffVendorTests (Task 4) proves output |
| T-02-CSS | Tampering | Bundled CSS loaded into WKWebView (later plan) | accept | Static, repo-owned, inlined offline; no remote loads — risk surfaces in Markdown plan, not here |
</threat_model>

<verification>
- `xcodebuild -resolvePackageDependencies -project Flint.xcodeproj -scheme Flint` succeeds.
- `xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS'` succeeds with vendored SwiftDiff + CSS resource in the target.
- `xcodebuild test ... -only-testing:FlintTests/SwiftDiffVendorTests` passes.
</verification>

<success_criteria>
ChromaKit 0.1.1 and swift-markdown (≥0.8.0) resolve; SwiftDiff is vendored, Swift-6-clean, and test-proven; the GitHub CSS is bundled. No existing package pin changed. The five tool plans can now import their dependencies without touching package resolution.
</success_criteria>

<output>
Create `.planning/phases/02-extended-tools/02-01-SUMMARY.md` when done.
</output>
